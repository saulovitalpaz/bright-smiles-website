# Anatomical Facial Application Map — Implementation Plan

1. Add failing FaceMap interaction and compatibility tests.
2. Replace the faceted mask with local organic SVG anatomy and canonical interactive region groups.
3. Add accessible text controls, notes-aware filled state, responsive dialog fields, and read-only summary behavior.
4. Add the optional compact presentation and remove transform scaling from EvolutionTimeline.
5. Run focused tests, lint, responsive contracts, production build, independent review, commit, and include in the final push to `main`.
