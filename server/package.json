{
    "name": "bright-smiles-server",
    "version": "1.0.0",
    "description": "Backend API for Bright Smiles",
    "main": "index.js",
    "scripts": {
        "build": "npx prisma generate",
        "start": "npx prisma generate && npx prisma db push --accept-data-loss && node seed.js && node index.js",
        "dev": "nodemon index.js"
    },
    "dependencies": {
        "@prisma/client": "^7.2.0",
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "multer": "^2.0.2",
        "pg": "^8.11.3"
    },
    "devDependencies": {
        "nodemon": "^3.0.1",
        "prisma": "^7.2.0"
    }
}